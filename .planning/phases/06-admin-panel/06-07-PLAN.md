---
phase: 06-admin-panel
plan: 07
type: execute
wave: 3
depends_on: ["06-02", "06-03", "06-04", "06-05", "06-06"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Admin panel is fully functional and verified by human"
    - "All CRUD operations work correctly"
    - "Authorization blocks non-admin users"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for the complete admin panel.

Purpose: Ensure all admin functionality works correctly before marking phase complete.

Output: User approval or list of issues to fix.
</objective>

<execution_context>
@/Users/shoemoney/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shoemoney/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete admin panel with post management, comment moderation, taxonomy management, and user management</what-built>
  <how-to-verify>
1. **Authentication Test**
   - Visit http://localhost:8000/admin (should redirect to /login)
   - Log in with jeremy@shoemoney.com credentials
   - Should land on admin dashboard

2. **Dashboard Verification**
   - Verify dashboard shows correct counts (posts, pending comments, users)
   - Click each sidebar link to verify navigation works

3. **Post Management** (ADMN-01)
   - Go to Posts section
   - Verify posts list with pagination
   - Test search functionality
   - Create a new test post (draft status)
   - Edit the test post, change to published
   - Verify post appears on frontend
   - Delete the test post

4. **Comment Moderation** (ADMN-02)
   - Go to Comments section
   - Verify pending comments queue (if any)
   - Test status filter tabs
   - Approve a comment (if pending available)
   - Mark a comment as spam
   - Delete a spam comment

5. **Category Management** (ADMN-03)
   - Go to Categories section
   - Create a test category
   - Edit the test category
   - Delete the test category (should work if no posts)

6. **Tag Management** (ADMN-03)
   - Go to Tags section
   - Create a test tag
   - Edit the test tag
   - Delete the test tag

7. **User Management** (ADMN-04)
   - Go to Users section
   - Verify user list shows admin badges correctly
   - Create a test user (non-admin)
   - Edit the test user
   - Toggle admin status on test user
   - Delete the test user

8. **Authorization Test** (Role-based access)
   - Log out
   - Create or use a non-admin user
   - Try to access /admin
   - Should get 403 Forbidden

9. **Overall UX**
   - Sidebar navigation is intuitive
   - Flash messages appear after actions
   - Forms show validation errors correctly
   - Pagination works on all list pages
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe specific issues found</resume-signal>
</task>

</tasks>

<verification>
- [ ] All admin sections accessible to admin user
- [ ] Non-admin users blocked with 403
- [ ] CRUD operations work for all entities
- [ ] Flash messages confirm actions
- [ ] Navigation is intuitive
</verification>

<success_criteria>
1. Human approves admin panel functionality
2. All ADMN requirements verified working
3. Authorization properly enforced
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-panel/06-07-SUMMARY.md`
</output>
