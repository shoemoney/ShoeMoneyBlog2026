---
phase: 03-public-content-display
plan: 08
type: execute
wave: 4
depends_on: ["03-04", "03-05", "03-06", "03-07"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Homepage displays paginated blog listing"
    - "Single posts display with full content and typography"
    - "Category and tag archives show filtered posts"
    - "Static pages display without post metadata"
    - "ShoeMoney branding is visible and consistent"
---

<objective>
Visual verification of all public content display features.

Purpose: Confirm the complete public-facing blog works as expected before Phase 3 completion.
Output: User approval of visual appearance and functionality.
</objective>

<execution_context>
@/Users/shoemoney/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shoemoney/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and verify all routes</name>
  <files></files>
  <action>
1. Ensure the development server is running:
   - php artisan serve (if not already running)
   - npm run dev (for Vite asset compilation)

2. Test each route programmatically to confirm no 500 errors:
   - curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/ (should be 200)
   - curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/category/uncategorized/ (200 or valid category)
   - Find a valid post URL from database and test
   - Find a valid page URL from database and test

3. If any return 500, diagnose and report the error.
  </action>
  <verify>
All route tests return 200 status codes
  </verify>
  <done>Dev server running and all routes returning 200</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete public content display system:
- Homepage with paginated blog listing
- Single post view with typography-styled content
- Category archive pages
- Tag archive pages
- Static pages (About, Contact)
- ShoeMoney navigation and footer
  </what-built>
  <how-to-verify>
1. Visit http://localhost:8000/ (homepage)
   - Verify "ShoeMoney" branding in navigation
   - Verify posts display with title, excerpt, date, author
   - Verify pagination appears at bottom
   - Click a post title to continue...

2. On single post page:
   - Verify title, author, date, reading time display
   - Verify content has proper typography (headings, paragraphs, links styled)
   - Verify categories display as blue pills
   - Verify tags display at bottom with hashtags
   - Use browser back button...

3. Click a category link from post:
   - Verify category name displays as heading
   - Verify filtered posts appear
   - Verify pagination works

4. Test /tag/{slug}/ for any known tag:
   - Verify tag archive displays similarly

5. Visit /about/ (or another static page):
   - Verify simpler layout (no author, date, categories)
   - Verify content is styled
   - Verify narrower width than blog posts

6. Check footer appears on all pages with copyright
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks good, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- All page types render without errors
- Typography styling makes content readable
- Navigation and footer consistent across pages
- SEO meta tags visible in page source
</verification>

<success_criteria>
- User confirms visual appearance is acceptable
- All ROADMAP success criteria for Phase 3 met:
  1. Paginated homepage with post excerpts
  2. Single post with full content
  3. Category archive pages
  4. Tag archive pages
  5. Static pages with distinct layout
  6. ShoeMoney brand identity visible
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-content-display/03-08-SUMMARY.md`
</output>
