---
phase: 03-public-content-display
plan: 07
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - resources/views/pages/show.blade.php
  - app/Http/Controllers/PageController.php
autonomous: true

must_haves:
  truths:
    - "User can view static pages with distinct layout from posts"
    - "Static pages display full content without post metadata"
    - "About and Contact pages render correctly"
  artifacts:
    - path: "resources/views/pages/show.blade.php"
      provides: "Static page view with simpler layout"
      contains: "rendered_content"
    - path: "app/Http/Controllers/PageController.php"
      provides: "Controller returning Blade view"
      contains: "view('pages.show'"
  key_links:
    - from: "app/Http/Controllers/PageController.php"
      to: "resources/views/pages/show.blade.php"
      via: "view() helper"
      pattern: "view\\('pages\\.show'"
---

<objective>
Build static page view with distinct styling from blog posts.

Purpose: Display About, Contact, and other static pages without blog post metadata.
Output: Static page view with simpler layout, updated PageController.
</objective>

<execution_context>
@/Users/shoemoney/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shoemoney/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-public-content-display/03-RESEARCH.md
@.planning/phases/03-public-content-display/03-02-SUMMARY.md
@.planning/phases/03-public-content-display/03-03-SUMMARY.md

Reference files:
@app/Http/Controllers/PageController.php
@app/Models/Page.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create static page view</name>
  <files>resources/views/pages/show.blade.php</files>
  <action>
Create resources/views/pages/show.blade.php (mkdir pages dir if needed):

1. Wrap in <x-layout> component
2. Article wrapper with max-w-3xl mx-auto (narrower than posts for focused reading)
3. Simple header:
   - H1 with page title (text-4xl font-bold text-gray-900 mb-8)
   - NO author, date, reading time, categories, tags (this is what makes it distinct from posts)
4. Content div with prose styling:
   ```
   prose prose-lg prose-slate max-w-none dark:prose-invert
   ```
   - Use {!! $page->rendered_content !!}

Key difference from posts/show.blade.php:
- Narrower container (max-w-3xl vs max-w-4xl)
- No metadata row (author, date, reading time)
- No categories or tags sections
- Cleaner, more focused presentation
  </action>
  <verify>
File exists at resources/views/pages/show.blade.php
Does NOT contain author or published_at references
Contains prose classes
  </verify>
  <done>Static page view displays content without blog post metadata</done>
</task>

<task type="auto">
  <name>Task 2: Update PageController to return view</name>
  <files>app/Http/Controllers/PageController.php</files>
  <action>
Update app/Http/Controllers/PageController.php:

1. Add import:
   - use Illuminate\View\View;
   - use Illuminate\Support\Str;

2. Change show() return type from JsonResponse to View

3. Update show() method:
   - Keep existing query with firstOrFail()
   - Add SEO before return:
     ```php
     seo()
         ->title($page->title . ' - ShoeMoney')
         ->description(Str::limit(strip_tags($page->content), 160));
     ```
   - Return: return view('pages.show', compact('page'));

4. Remove JSON placeholder response.
  </action>
  <verify>
curl http://localhost:8000/about/ returns HTML (assuming "about" page exists)
Page shows title and content without post metadata
  </verify>
  <done>Static page route returns Blade view with page content</done>
</task>

</tasks>

<verification>
- Visit /about/ (if exists) - shows page title and content
- Visit /contact/ (if exists) - shows page title and content
- Pages do NOT show author, date, reading time, categories, or tags
- Content styled with prose typography
- Layout is visually distinct (narrower width) from blog posts
</verification>

<success_criteria>
- Static pages display title and content only
- No blog post metadata visible (author, date, categories, tags)
- Narrower content width for focused reading
- Shortcodes processed via rendered_content accessor
- SEO meta tags include page title
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-content-display/03-07-SUMMARY.md`
</output>
