---
phase: 03-public-content-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - resources/css/app.css
  - config/seo.php
autonomous: true

must_haves:
  truths:
    - "Tailwind Typography plugin is installed and available"
    - "SEO package is installed and configured"
    - "CSS includes brand color variables"
  artifacts:
    - path: "package.json"
      provides: "@tailwindcss/typography dependency"
      contains: "@tailwindcss/typography"
    - path: "resources/css/app.css"
      provides: "Typography plugin and brand colors"
      contains: "@plugin"
    - path: "config/seo.php"
      provides: "SEO configuration"
---

<objective>
Install frontend packages and configure Tailwind CSS for content display.

Purpose: Enable prose styling for WordPress content and SEO meta tags for all pages.
Output: Typography plugin installed, CSS configured with brand colors, SEO package ready.
</objective>

<execution_context>
@/Users/shoemoney/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shoemoney/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-public-content-display/03-RESEARCH.md

Reference files:
@package.json
@resources/css/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install npm and composer packages</name>
  <files>package.json, composer.json</files>
  <action>
Install the required packages:

1. npm install -D @tailwindcss/typography
2. composer require archtechx/laravel-seo

Verify package.json includes @tailwindcss/typography in devDependencies.
Verify composer.json includes archtechx/laravel-seo in require.
  </action>
  <verify>
npm list @tailwindcss/typography shows version
composer show archtechx/laravel-seo shows installed
  </verify>
  <done>Both packages installed and listed in respective package manifests</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind CSS with Typography plugin and brand colors</name>
  <files>resources/css/app.css</files>
  <action>
Update resources/css/app.css to:

1. Add @plugin "@tailwindcss/typography" directive (Tailwind v4 syntax)
2. Add ShoeMoney brand color variables in @theme block:
   - --color-brand-primary: #1e40af (blue-800)
   - --color-brand-accent: #f59e0b (amber-500)
   - --color-brand-light: #f8fafc (slate-50)
3. Add custom styles for WordPress content elements:
   - .wp-caption for figure/figcaption styling
   - .wp-video for video elements
   - .wp-more for read-more breaks

Keep existing @source directives and font configuration.
  </action>
  <verify>
npm run build completes without errors
Inspect built CSS for prose classes
  </verify>
  <done>CSS builds successfully with typography plugin and brand colors</done>
</task>

<task type="auto">
  <name>Task 3: Publish and configure SEO package</name>
  <files>config/seo.php</files>
  <action>
1. php artisan seo:install (if available) or php artisan vendor:publish --tag=seo-config
2. Configure config/seo.php with:
   - site_name: 'ShoeMoney'
   - title_suffix: ' - ShoeMoney'
   - default description: 'The original blog about making money online since 2003'
   - Twitter card type: 'summary_large_image'
   - Default image: null (will be set per-page)
  </action>
  <verify>
php artisan config:show seo displays configuration
config/seo.php file exists with ShoeMoney settings
  </verify>
  <done>SEO package configured with ShoeMoney defaults</done>
</task>

</tasks>

<verification>
- npm list @tailwindcss/typography returns valid version
- npm run build succeeds
- composer show archtechx/laravel-seo shows package info
- config/seo.php exists with site_name = 'ShoeMoney'
</verification>

<success_criteria>
- Typography plugin available for prose classes
- Brand colors defined as CSS variables
- SEO package ready for meta tag generation
- Build process completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-content-display/03-01-SUMMARY.md`
</output>
